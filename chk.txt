0. lagbe na
echo "# EntropyZero" >> README.md

step 01: git initialize kora ei step er kaj. need only one time for each project
        --> git init

step 02: git e file add kora. everytime lagbe.
        --> git add .

step 03:everytime you need to commit.
        --> git commit -m "first commit"

step 04: optional and easy 
        --> git branch -M main

step 05: upload local files on github repo
        --> git remote add origin https://github.com/nayancsbd/EntropyZero.git

step 06: at last not least. push your git
        --> git push -u origin main



git commit -m “prompt file added and almost done”
git branch -M main
git remote add origin https://github.com/nayancsbd/CodeCraft.git
git push -u origin main
